const path = require("path");

// aqui indicamos para o sequelize onde está os arquivos de configuração
module.exports = {
  config: path.resolve(__dirname, "src", "config", "database.js"),
  "migrations-path": path.resolve(__dirname, "src", "migrations"),
};